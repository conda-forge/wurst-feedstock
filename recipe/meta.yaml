{% set name = "wurst" %}
{% set version = "0.5.2" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 6651cdd3aac50aac6326a5a287172f42d20a100f8712ad9907793a65553d1c3a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - wurst-cli = wurst.bin.wurst_cli:main

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - constructive_geometries >=0.9.4
    - docopt
    - numpy
    - pandas
    - platformdirs
    - python >={{ python_min }}
    - python-json-logger
    - toolz
    - tqdm
    - wrapt

test:
  imports:
    - wurst
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://github.com/polca/wurst
  summary: Wurst is a python package for linking and modifying industrial ecology models
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE
  dev_url: https://github.com/polca/wurst

extra:
  recipe-maintainers:
    - m-rossi
